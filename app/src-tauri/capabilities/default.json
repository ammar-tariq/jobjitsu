{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal host capabilities. Dialog open is for choosing the on-device data folder only — no fs/shell/HTTP career egress (ADR 0013).",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
