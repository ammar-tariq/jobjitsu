{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal host capabilities for the main window. No filesystem, shell, or HTTP plugins — deny-by-default until IPC stories land (ADR 0013).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
