{
  "name": "@jobjitsu/agent",
  "version": "0.0.0",
  "private": true,
  "description": "Preparative agent orchestration contracts (never owns send)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@jobjitsu/core": "workspace:*",
    "@jobjitsu/events": "workspace:*",
    "@jobjitsu/preferences": "workspace:*",
    "@jobjitsu/ai": "workspace:*",
    "@jobjitsu/discovery": "workspace:*",
    "@jobjitsu/applications": "workspace:*",
    "@jobjitsu/queue": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src --max-warnings 0",
    "clean": "rm -rf dist coverage .turbo",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
